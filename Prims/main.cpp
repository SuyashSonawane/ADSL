#include <iostream>

using namespace std; 

#define MAX 999

int main()
{
	int mst[10],keys[10];
	bool isincluded[10];
	int V;
	cout<<"Enter number of vertex : ";
	cin>>V;
	for(int i=0;i<V;i++)
		keys[i]=MAX , isincluded[i]=false;

}

Dept maintians a student info  the file onain nroll num be , names , div and addrss allow user
 to add delete info of student , display info of particular info , if record doesnt exit an mess is displayed , if it is then 
 the sys displas the syudent details use sequential file 